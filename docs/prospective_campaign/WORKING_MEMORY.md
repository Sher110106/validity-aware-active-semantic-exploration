# Working memory

The authoritative contract is `SPEC.md`. Maintain future pilot ledger,
worst-case pair cost, selected horizon, reservations, technical stop reasons,
and terminal scope decision here or in append-only machine artifacts. Never
replace an existing attempt directory or infer outcomes from queue position.
